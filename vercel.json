{
  "version": 2,
  "buildCommand": "cd packages/nextjs && npm install --no-package-lock && npm run vercel-build",
  "outputDirectory": "packages/nextjs/.next",
  "installCommand": "cd packages/nextjs && npm install --no-scripts && npm run postinstall",
  "builds": [
    {
      "src": "packages/nextjs/package.json",
      "use": "@vercel/next"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "packages/nextjs/$1",
      "continue": true
    }
  ]
}
